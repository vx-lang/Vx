// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module

import std::io;
fn main() -> i32 {
  let strs = [ "red", "green", "blue" ];

  for sptr in strs.iter() {
    print!(sptr);
    print!(0);
  }
  return 0;
}
