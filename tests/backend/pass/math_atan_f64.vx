//===- math_atan_f64.vx - Vx Compiler -------------------*- Vx -*-===//
//
// Part of the Vx Project, under the Apache License v2.0 with LLVM Exceptions.
// See LICENSE for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK-DAG: func.func private @atan(
// CHECK-DAG: func.func private @fabs(
// CHECK-LABEL: func.func @f64$atan(%arg0: f64) -> f64
// CHECK: call @atan(%{{.*}}) : (f64) -> f64
// CHECK-LABEL: func.func @f64$abs(%arg0: f64) -> f64
// CHECK: call @fabs(%{{.*}}) : (f64) -> f64
// CHECK-LABEL: func.func @main() -> i32
// CHECK: call @f64$atan(%{{.*}}) : (f64) -> f64
// CHECK: call @f64$abs(%{{.*}}) : (f64) -> f64
import std::math;

fn main() -> i32 {
  let x : f64 = 0.0;
  let res : f64 = x.atan();
  let expected : f64 = 0.0;

  // allow small floating point difference
  let diff : f64 = (res - expected).abs();
  let mut ret : i32 = 1;
  if diff < 0.001 {
    ret = 0;
  }
  return ret;
}
