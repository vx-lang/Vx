// EXPECT: 42
extern {
  fn vx_printf_i32(format: *const i8, val: i32) -> i32;
}

fn main() -> i32 {
  let base = 42;
  let get_base = || base;
  let res = get_base();
  unsafe {
    vx_printf_i32("%d
", res);
  }
  return res;
}
