//===- math_intrinsics.vx - Vx Compiler --------------------------*- Vx -*-===//
//
// Part of the Vx Project, under the BSD 3-Clause License.
// See LICENSE for license information.
// SPDX-License-Identifier: BSD-3-Clause
//
//===----------------------------------------------------------------------===//
// RUN: vxc %s
// RUN: %t

import std::io;
extern "C" {
  fn vx_sqrtf(x: f32) -> f32;
  fn vx_expf(x: f32) -> f32;
}

fn main() -> i32 {
  let a: f32 = 16.0;
  let b: f32 = 0.0;

  unsafe {
    let root = vx_sqrtf(a);
    let exp = vx_expf(b);
    print_f32(root);
    print_f32(exp);
  }
  return 0;
}

// CHECK: 4.000000
// CHECK: 1.000000




// FRONTEND-LABEL: module {
// FRONTEND: func.func @test_f32() -> i32
// FRONTEND: memref.alloc

// MLIR-LABEL: module {
// MLIR: func.func @test_f32() -> i32

// LLVM-LABEL: module {
// LLVM: llvm.func @test_f32() -> i32
