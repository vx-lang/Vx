//===- math_sin_f32.vx - Vx Compiler -------------------*- Vx -*-===//
//
// Part of the Vx Project, under the BSD 3-Clause License.
// See LICENSE for license information.
// SPDX-License-Identifier: BSD-3-Clause
//
//===----------------------------------------------------------------------===//
// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK-DAG: func.func private @sinf(
// CHECK-DAG: func.func private @fabsf(
// CHECK-LABEL: func.func @f32$sin(%arg0: f32) -> f32
// CHECK: call @sinf(%{{.*}}) : (f32) -> f32
// CHECK-LABEL: func.func @f32$abs(%arg0: f32) -> f32
// CHECK: call @fabsf(%{{.*}}) : (f32) -> f32
// CHECK-LABEL: func.func @main() -> i32
// CHECK: call @f32$sin(%{{.*}}) : (f32) -> f32
// CHECK: call @f32$abs(%{{.*}}) : (f32) -> f32
import std::math;

fn main() -> i32 {
  let x : f32 = 0.0;
  let res : f32 = x.sin();
  let expected : f32 = 0.0;

  // allow small floating point difference
  let diff : f32 = (res - expected).abs();
  let mut ret : i32 = 1;
  if diff < 0.001 {
    ret = 0;
  }
  return ret;
}
