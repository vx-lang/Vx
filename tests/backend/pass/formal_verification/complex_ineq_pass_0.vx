//===- complex_ineq_pass_0.vx ---------------------------------===//
// RUN: vxc %s


fn test_complex_0(X: i32, Y: i32, Z: i32) -> i32
requires X >= Y && Y >= Z
ensures return >= 0
{
    let diff = X - Z;
    return diff;
}

fn main() -> i32 { return 0; }

