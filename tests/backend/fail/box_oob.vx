// RUN: not vxc %s | FileCheck %s
// CHECK: Caught SIGSEGV
// CHECK: Backtrace:
import std::box;

fn main() -> i32 {
  let mut b = Box<i32>::new(42);
  unsafe {
    let ptr = b.ptr;
    // deref past the allocation
    let bad = ptr[100000000];
    print!(bad);
  }
  return 0;
}
