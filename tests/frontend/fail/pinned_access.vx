//===- pinned_access.vx - Vx Compiler ------------------------------------*- Vx -*-===//
// RUN: not vxc %s --action print-ast 2>&1 | FileCheck %s

fn main(device_data: Pinned<DynTensor<f32>, Topology::CPU>) on Topology::NPU[0] -> i32 {
  let x = device_data[0];
  return 0;
}

// The message no longer names `Pinned`. The check reads where a value is from
// either spelling -- the wrapper, or a placement on the tensor itself -- so
// naming one of them described the type rather than the mistake.
// CHECK: Cross-topology access error: a value on CPU cannot be read from NPU
