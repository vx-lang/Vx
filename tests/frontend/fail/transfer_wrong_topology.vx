//===- transfer_wrong_topology.vx - Vx Compiler --------------------------*- Vx -*-===//
// RUN: not vxc %s --action print-ast 2>&1 | FileCheck %s

fn main(host_data: Tensor<f32>) on Topology::NPU[0] -> i32 {
    let device_data = transfer(host_data, Memory::CPU_DRAM);
    let x = device_data[0];
    return 0;
}

// CHECK: Error: Cross-topology access error
