// RUN: not vxc %s 2>&1 | FileCheck %s
// CHECK: Function 'tile' cannot prove postcondition (ensures) at compile time
fn tile(N : i32) -> i32
requires N > 0
ensures return > 10 {
  return N;
}

fn main() -> i32 {
  let result = tile(5);
  return result;
}
