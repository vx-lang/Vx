//===- math_fail.vx ---------------------------------===//
fn test_math_fail_0(N: i32) -> i32
requires N > 83
ensures return > 121
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 38 - 1;
    return x;
}

fn test_math_fail_1(N: i32) -> i32
requires N > 19
ensures return > 53
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 34 - 1;
    return x;
}

fn test_math_fail_2(N: i32) -> i32
requires N > 35
ensures return > 63
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 28 - 1;
    return x;
}

fn test_math_fail_3(N: i32) -> i32
requires N > 62
ensures return > 76
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 14 - 1;
    return x;
}

fn test_math_fail_4(N: i32) -> i32
requires N > 94
ensures return > 144
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 50 - 1;
    return x;
}

fn test_math_fail_5(N: i32) -> i32
requires N > 87
ensures return > 90
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 3 - 1;
    return x;
}

fn test_math_fail_6(N: i32) -> i32
requires N > 17
ensures return > 55
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 38 - 1;
    return x;
}

fn test_math_fail_7(N: i32) -> i32
requires N > 41
ensures return > 50
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 9 - 1;
    return x;
}

fn test_math_fail_8(N: i32) -> i32
requires N > 80
ensures return > 91
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 11 - 1;
    return x;
}

fn test_math_fail_9(N: i32) -> i32
requires N > 73
ensures return > 84
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 11 - 1;
    return x;
}

fn test_math_fail_10(N: i32) -> i32
requires N > 66
ensures return > 98
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 32 - 1;
    return x;
}

fn test_math_fail_11(N: i32) -> i32
requires N > 82
ensures return > 87
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 5 - 1;
    return x;
}

fn test_math_fail_12(N: i32) -> i32
requires N > 16
ensures return > 37
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 21 - 1;
    return x;
}

fn test_math_fail_13(N: i32) -> i32
requires N > 52
ensures return > 80
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 28 - 1;
    return x;
}

fn test_math_fail_14(N: i32) -> i32
requires N > 48
ensures return > 51
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 3 - 1;
    return x;
}

fn test_math_fail_15(N: i32) -> i32
requires N > 69
ensures return > 70
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 1 - 1;
    return x;
}

fn test_math_fail_16(N: i32) -> i32
requires N > 90
ensures return > 107
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 17 - 1;
    return x;
}

fn test_math_fail_17(N: i32) -> i32
requires N > 36
ensures return > 81
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 45 - 1;
    return x;
}

fn test_math_fail_18(N: i32) -> i32
requires N > 77
ensures return > 102
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 25 - 1;
    return x;
}

fn test_math_fail_19(N: i32) -> i32
requires N > 24
ensures return > 66
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 42 - 1;
    return x;
}

fn test_math_fail_20(N: i32) -> i32
requires N > 36
ensures return > 69
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 33 - 1;
    return x;
}

fn test_math_fail_21(N: i32) -> i32
requires N > 11
ensures return > 36
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 25 - 1;
    return x;
}

fn test_math_fail_22(N: i32) -> i32
requires N > 40
ensures return > 59
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 19 - 1;
    return x;
}

fn test_math_fail_23(N: i32) -> i32
requires N > 17
ensures return > 62
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 45 - 1;
    return x;
}

fn test_math_fail_24(N: i32) -> i32
requires N > 99
ensures return > 134
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 35 - 1;
    return x;
}

fn test_math_fail_25(N: i32) -> i32
requires N > 88
ensures return > 93
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 5 - 1;
    return x;
}

fn test_math_fail_26(N: i32) -> i32
requires N > 71
ensures return > 85
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 14 - 1;
    return x;
}

fn test_math_fail_27(N: i32) -> i32
requires N > 36
ensures return > 86
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 50 - 1;
    return x;
}

fn test_math_fail_28(N: i32) -> i32
requires N > 73
ensures return > 86
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 13 - 1;
    return x;
}

fn test_math_fail_29(N: i32) -> i32
requires N > 41
ensures return > 80
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 39 - 1;
    return x;
}

fn test_math_fail_30(N: i32) -> i32
requires N > 60
ensures return > 107
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 47 - 1;
    return x;
}

fn test_math_fail_31(N: i32) -> i32
requires N > 30
ensures return > 48
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 18 - 1;
    return x;
}

fn test_math_fail_32(N: i32) -> i32
requires N > 17
ensures return > 23
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 6 - 1;
    return x;
}

fn test_math_fail_33(N: i32) -> i32
requires N > 72
ensures return > 104
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 32 - 1;
    return x;
}

fn test_math_fail_34(N: i32) -> i32
requires N > 89
ensures return > 90
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 1 - 1;
    return x;
}

fn test_math_fail_35(N: i32) -> i32
requires N > 13
ensures return > 27
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 14 - 1;
    return x;
}

fn test_math_fail_36(N: i32) -> i32
requires N > 42
ensures return > 50
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 8 - 1;
    return x;
}

fn test_math_fail_37(N: i32) -> i32
requires N > 73
ensures return > 90
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 17 - 1;
    return x;
}

fn test_math_fail_38(N: i32) -> i32
requires N > 87
ensures return > 124
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 37 - 1;
    return x;
}

fn test_math_fail_39(N: i32) -> i32
requires N > 36
ensures return > 49
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 13 - 1;
    return x;
}

fn test_math_fail_40(N: i32) -> i32
requires N > 49
ensures return > 89
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 40 - 1;
    return x;
}

fn test_math_fail_41(N: i32) -> i32
requires N > 19
ensures return > 68
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 49 - 1;
    return x;
}

fn test_math_fail_42(N: i32) -> i32
requires N > 51
ensures return > 99
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 48 - 1;
    return x;
}

fn test_math_fail_43(N: i32) -> i32
requires N > 46
ensures return > 77
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 31 - 1;
    return x;
}

fn test_math_fail_44(N: i32) -> i32
requires N > 99
ensures return > 117
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 18 - 1;
    return x;
}

fn test_math_fail_45(N: i32) -> i32
requires N > 17
ensures return > 42
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 25 - 1;
    return x;
}

fn test_math_fail_46(N: i32) -> i32
requires N > 75
ensures return > 113
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 38 - 1;
    return x;
}

fn test_math_fail_47(N: i32) -> i32
requires N > 32
ensures return > 40
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 8 - 1;
    return x;
}

fn test_math_fail_48(N: i32) -> i32
requires N > 59
ensures return > 80
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 21 - 1;
    return x;
}

fn test_math_fail_49(N: i32) -> i32
requires N > 66
ensures return > 101
{
    // FAILS because x might be less than val + add_val if N is exactly val+1
    let x = N + 35 - 1;
    return x;
}

fn main() -> i32 { return 0; }
