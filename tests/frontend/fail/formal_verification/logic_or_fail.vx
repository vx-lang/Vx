//===- logic_or_fail.vx ---------------------------------===//
fn test_logic_or_fail_0(A: i32, B: i32) -> i32
requires A > 49 || B > 58
ensures return > 107
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_1(A: i32, B: i32) -> i32
requires A > 22 || B > 19
ensures return > 41
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_2(A: i32, B: i32) -> i32
requires A > 92 || B > 41
ensures return > 133
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_3(A: i32, B: i32) -> i32
requires A > 89 || B > 68
ensures return > 157
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_4(A: i32, B: i32) -> i32
requires A > 78 || B > 95
ensures return > 173
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_5(A: i32, B: i32) -> i32
requires A > 62 || B > 71
ensures return > 133
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_6(A: i32, B: i32) -> i32
requires A > 73 || B > 89
ensures return > 162
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_7(A: i32, B: i32) -> i32
requires A > 62 || B > 88
ensures return > 150
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_8(A: i32, B: i32) -> i32
requires A > 63 || B > 3
ensures return > 66
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_9(A: i32, B: i32) -> i32
requires A > 5 || B > 35
ensures return > 40
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_10(A: i32, B: i32) -> i32
requires A > 37 || B > 49
ensures return > 86
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_11(A: i32, B: i32) -> i32
requires A > 11 || B > 10
ensures return > 21
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_12(A: i32, B: i32) -> i32
requires A > 92 || B > 53
ensures return > 145
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_13(A: i32, B: i32) -> i32
requires A > 29 || B > 62
ensures return > 91
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_14(A: i32, B: i32) -> i32
requires A > 55 || B > 8
ensures return > 63
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_15(A: i32, B: i32) -> i32
requires A > 76 || B > 40
ensures return > 116
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_16(A: i32, B: i32) -> i32
requires A > 30 || B > 72
ensures return > 102
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_17(A: i32, B: i32) -> i32
requires A > 85 || B > 46
ensures return > 131
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_18(A: i32, B: i32) -> i32
requires A > 46 || B > 84
ensures return > 130
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_19(A: i32, B: i32) -> i32
requires A > 25 || B > 79
ensures return > 104
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_20(A: i32, B: i32) -> i32
requires A > 54 || B > 61
ensures return > 115
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_21(A: i32, B: i32) -> i32
requires A > 85 || B > 29
ensures return > 114
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_22(A: i32, B: i32) -> i32
requires A > 40 || B > 23
ensures return > 63
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_23(A: i32, B: i32) -> i32
requires A > 76 || B > 91
ensures return > 167
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_24(A: i32, B: i32) -> i32
requires A > 17 || B > 75
ensures return > 92
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_25(A: i32, B: i32) -> i32
requires A > 83 || B > 62
ensures return > 145
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_26(A: i32, B: i32) -> i32
requires A > 28 || B > 80
ensures return > 108
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_27(A: i32, B: i32) -> i32
requires A > 36 || B > 72
ensures return > 108
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_28(A: i32, B: i32) -> i32
requires A > 86 || B > 62
ensures return > 148
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn test_logic_or_fail_29(A: i32, B: i32) -> i32
requires A > 74 || B > 82
ensures return > 156
{
    // FAILS because one of them could be negative or less than the required amount
    let sum = A + B;
    return sum;
}

fn main() -> i32 { return 0; }
