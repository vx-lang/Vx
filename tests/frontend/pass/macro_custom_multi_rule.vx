// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;

macro_rules do_thing { ($x : expr) =>{
    $x + 1
  }; }
fn main() -> i32 {
  let a = do_thing!(5);
  return a;
}