// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;
macro_rules square { ($x : expr) =>{
    $x * $x
  }; }
fn main() -> i32 {
  return square!(5);
}
