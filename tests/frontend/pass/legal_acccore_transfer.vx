//===- legal_acccore_transfer.vx - Vx Compiler ---------------------------*- Vx -*-===//
//
// Part of the Vx Project, under the BSD 3-Clause License.
// See LICENSE for license information.
// SPDX-License-Identifier: BSD-3-Clause
//
//===----------------------------------------------------------------------===//
// RUN: vxc %s | FileCheck %s

fn main() -> i32 {
  let t_host: Tensor<f32> = 1.0;
  let t_npu = transfer(t_host, Memory::NPUHBM);
  
  spawn on (Topology::AccCore[0]) {
    // CHECK-NOT: error
    // Legal transfer from NPU HBM to Local SRAM
    let t_sram = transfer(t_npu, Memory::LocalSRAM);
    let valid = t_sram;
  }

  return 0;
}
