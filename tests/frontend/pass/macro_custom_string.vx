// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;

macro_rules get_str { ($s : expr) => {
    $s
  }; }
fn main() -> i32 {
  let s = get_str!("hello");
  return 0;
}