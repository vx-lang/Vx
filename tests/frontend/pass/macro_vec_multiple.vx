// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;

fn main() -> i32 {
  let mut v: Vec<i32> = vec![10, 20, 30];
  assert(v.len == 3);
  assert(v[0] == 10);
  assert(v[1] == 20);
  assert(v[2] == 30);
  return 0;
}
