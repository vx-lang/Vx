// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// RUN: %t 2>&1 | FileCheck %s

import std::tensor;

fn main() -> i32 {
  let mut x : Tensor = Tensor<f32>([ 10, 10 ]);
  x.fill(1.0);
  let y = x.map(| val | val * 2.0);
  return 0;
}

// CHECK: module
