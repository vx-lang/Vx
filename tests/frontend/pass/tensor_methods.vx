// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s

import std::tensor;

fn main() -> i32 {
  let mut x = Tensor<f32>([ 10, 10 ]);
  x.fill(1.0);
  let y = x.map(| val | val * 2.0);
  return 0;
}

// main calls each function in order; the assertion is that the chain survives to codegen
// rather than being folded away.
// CHECK-LABEL: func.func @main() -> i32
// CHECK: call @Tensor$f32$10x10$fill$f32(%{{.*}}, %{{.*}}) : (memref<memref<?x?xf32>>, f32) -> ()
