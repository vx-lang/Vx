// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;

macro_rules shape_zero { ($t : expr) =>{$t.shape[0]}; }
fn main() -> i32 {
  let t : Tensor = Tensor<f32>([ 10, 20 ]);
  let s = shape_zero !(t);
  return s;
}