// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
struct Array<T, const N: i32> {
  val: T
}

fn initialize<const N: i32>() -> Array<f32, N> {
  let a = Array<f32, N> {
    val: 1.0
  };
  return a;
}

fn test_const_generics() -> void {
  let array = initialize<10>();
  assert(true);
}
