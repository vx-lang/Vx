import std::vec;

fn main() -> i32 {
    let mut v: Vec<Vec<i32>> = vec![vec![1, 2], vec![3, 4]];
    assert(v.len == 2);
    assert(v[0][0] == 1);
    assert(v[0][1] == 2);
    assert(v[1][0] == 3);
    assert(v[1][1] == 4);
    return 0;
}
