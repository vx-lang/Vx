// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;

macro_rules first { ($arr : expr) =>{
    $arr[0]
  }; }
fn main() -> i32 {
  let arr = [ 10, 20, 30 ];
  let f = first!(arr);
  return f;
}