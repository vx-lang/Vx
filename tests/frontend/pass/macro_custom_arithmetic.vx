// RUN: vxc %s --action emit-mlir 2>&1 | FileCheck %s
// CHECK: module
import std::vec;
import std::closure;

macro_rules double {
  ($x:expr) => { $x * 2 };
}
fn main() -> i32 {
  let a = double!(21);
  return a;
}