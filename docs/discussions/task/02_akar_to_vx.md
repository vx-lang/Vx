# GitHub Release Cleanup Tasks

- `[x]` Update `Cargo.toml` with author, description, repository, and license fields.
- `[x]` Update `.gitignore` with rules for `.dylib`, `.o`, `*.log`, `*.so`, and old binaries.
- `[x]` Delete `GEMINI.md` and `run_ref.c` from the repository.
- `[x]` Move `Proposal.md` into `docs/`.
- `[x]` Create `scripts/legacy/` and move Python refactoring scripts into it.
- `[ ]` Commit the cleanup changes to git.
