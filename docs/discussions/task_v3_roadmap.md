- `[ ]` Add missing libc bindings to `stdlib/std/fs.vx` (like `mmap`, `open`, `fstat`).
- `[ ]` Implement high-level file mapping and reading methods in `std::fs::File`.
- `[ ]` Enhance `std::string` with required string manipulation routines (`eq`, `starts_with`, `from_bytes`).
- `[ ]` Rewrite `vx_load_config` natively in `llama2_v2.vx` (or a utility file).
- `[ ]` Rewrite `vx_load_weights` natively using `mmap`.
- `[ ]` Rewrite `vx_build_tokenizer` and `vx_decode_token` using native file reading and string operations.
- `[ ]` Rewrite `vx_encode_prompt` and `vx_read_prompt_file`.
- `[ ]` Build and run the `llama2_v2.vx` test to verify correctness without C FFI.
