- `[x]` Expose `registerVxPassesC` in `src/dialect/VxLowering.cpp`
- `[x]` Add `registerVxPasses` FFI in `src/codegen/mod.rs`
- `[x]` Update `src/driver.rs` to replace `--disable-optimizations` with `-O` flag
- [x] Fix compile_test pipeline failure
  - [x] Update `src/driver.rs` to group passes correctly into `func.func`
  - [x] Relax FileCheck to work with new natively generated AST
- `[x]` Parameterize `run_benchmark(N)` dimensions (128, 256, 512, 1024, 2048) in fusion benchmark scripts
- `[x]` Address MLIR topological execution errors in benchmarks
- `[x]` Restore Llama2 benchmark syntax to allow lowering without compilation segfaults
- `[x]` Integrate `apply_mlir_opt` pipeline (with LLVM IR optimization) in the `vxc` JIT driver
- `[x]` Benchmark execution times to verify actual `time_fused` vs `time_unfused` on NPU/CPU and measure impact of `-O3` pipeline AST

## Phase 4: Benchmarking Loop Fusion

- [x] Create `tests/benchmarks/cpu_fusion.vx` (matmul + bias + ReLU)
- [x] Create `tests/benchmarks/npu_fusion.vx` (same logic mapped to NPU)
- [x] Measure and print latencies using `-O0` (unfused) and `-O3` (fused) modes
- [x] Verify functionality via `cargo test --test compile_test` and manual JIT tests
