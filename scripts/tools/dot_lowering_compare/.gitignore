# The repo root ignores *.mlir (generated IR). The .mlir files HERE are not
# generated -- they are this example's hand-written sources, and without them
# compare.sh and the bench flow have nothing to compile.
!two_dots.mlir
!bench.mlir

# These ARE generated.
*.ptx
nvvm.mlir
